import {Layout} from '../components';

export default function About(props) {
    return (
        <Layout currentPage="about" title="About - Slate" {...props}>
            about
        </Layout>
    );
}
